# Test Smells Report — 2026-04-14

## Summary

| Smell | Count | Status |
|-------|-------|--------|
| `expect(true).toBe(true)` | 3 → 0 | ✅ FIXED |
| Files without `expect()` | 0 | ✅ Clean |
| `.skip` / `.todo` | 0 | ✅ Clean |
| `@ts-ignore` in tests | 0 | ✅ Clean |
| `as any` in tests | 30 occurrences | ⚠️ Acceptable (mock fixtures) |
| Trivial placeholder test | 1 → 0 | ✅ FIXED (example.test.ts) |
| Failing tests | 2 → 0 | ✅ FIXED (usePartners) |
| Pre-existing failures | 1 | ⚠️ state-enum-integrity (DB timeout) |

## Fixes Applied

### 1. `expect(true).toBe(true)` — 3 occurrences removed

| File | Line | Fix |
|------|------|-----|
| `bug-registry-confirmation.test.ts:164` | BF-003 shadowing | Replaced with `typeof` assertions on actual values |
| `bug-registry-confirmation.test.ts:493` | BF-009 race condition | Replaced with documented string assertion |
| `example.test.ts:5` | Placeholder | Replaced with real alias resolution test |

### 2. Failing tests — 2 fixed

| File | Issue | Fix |
|------|-------|-----|
| `usePartners.test.ts` | `queryKeys.partners.byCountry` not in mock | Added `byCountry` to queryKeys mock |

### 3. `as any` usage in tests (30 occurrences)

All in mock/fixture context — acceptable per testing conventions:
- `AgentSignatureConfig.test.tsx` — mock agent object
- `AgentVoiceCall.test.tsx` — mock agent prop
- `DrawerPresetManager.test.tsx` — mock presets array
- `useAIConversation.test.ts` — mock auth.getUser
- `useAgents.test.ts` — mock auth.getUser
- `useAppSettings.test.ts` — mock auth.getUser
- `useContacts.test.ts` — mock findContacts return

**Recommendation**: Create typed mock factories (`createMockAgent()`, `createMockUser()`) to eliminate `as any` in future tests.

## Pre-existing Issues (not fixed this round)

### state-enum-integrity.test.ts
- **Issue**: DB query timeout in `partners.lead_status` validation
- **Root cause**: Full table scan on production data (statement timeout)
- **Fix**: Add `.limit(100)` or use enum metadata query instead

---
*Generated: 2026-04-14*
