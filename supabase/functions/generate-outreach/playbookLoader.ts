/**
 * generate-outreach/playbookLoader.ts — Thin re-export shim.
 *
 * Logica reale in `_shared/playbookLoader.ts` (SSOT, audit Sez.3).
 */
export { loadActivePlaybook, type ActivePlaybookResult } from "../_shared/playbookLoader.ts";
